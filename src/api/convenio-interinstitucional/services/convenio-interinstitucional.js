'use strict';

/**
 * convenio-interinstitucional service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::convenio-interinstitucional.convenio-interinstitucional');
